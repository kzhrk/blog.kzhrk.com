box: node:latest
build:
  steps:
    - npm-install
    - arjen/hugo-build:
        version: 0.38
    - script:
        name: Build
        code: make build
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: blog.kzhrk.com
        basedir: public
